name: CI

on:
  push:
  workflow_dispatch:

jobs:
  setup:
    uses: ./.github/workflows/reuse.yml

  main:
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - uses: actions/checkout@v4

      - name: Node version
        run: node --version
